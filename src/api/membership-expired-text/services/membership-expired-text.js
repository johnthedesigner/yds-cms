'use strict';

/**
 * membership-expired-text service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::membership-expired-text.membership-expired-text');
